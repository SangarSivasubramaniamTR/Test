name: Testing

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name'     
        required: true
     
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, ${{ inputs.name }}!"
